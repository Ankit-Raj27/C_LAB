#include <stdio.h>
#include <string.h>
int main()
{
    char src[100], dst[100];
    int i=0;
    
    printf("Enter source string : ");
    scanf("%s",src);

    while(dst[i] = src[i++]);

    printf("\nDestination String : %s", dst);
    return 0; 
}